{
  "name": "demo-49-js-minify-compare",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:esbuild": "cross-env MINIFY=esbuild vite build",
    "build:terser": "cross-env MINIFY=terser vite build",
    "build:none": "cross-env MINIFY=false vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "terser": "^5.27.0",
    "vite": "^5.4.0"
  }
}
